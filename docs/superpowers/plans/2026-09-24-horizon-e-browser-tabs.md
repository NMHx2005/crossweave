# Horizon E Remaining — Browser Tabs Plan

**Goal:** Browser tabs on web stage strip.

**Spec:** `docs/superpowers/specs/2026-09-24-horizon-e-browser-tabs-design.md`

**Tasks:**
1. Add tab strip to `src/gateway/web/index.html` — JS tabs state + render + pin/close/reorder, + file explorer wiring to `wc.openFile`.
2. Test via manual browser or `bun test` for client helper if added.

**Gate:** typecheck · build
